services:
  ytclip:
    build: .
    image: ytclip:latest
    container_name: ytclip
    ports:
      - "8899:8899"
    volumes:
      - ytclip-downloads:/app/downloads
    restart: unless-stopped

volumes:
  ytclip-downloads:
